## What does this change?

## Why

## Testing

- [ ] `go build ./...` passes
- [ ] `go vet ./...` passes
- [ ] `go test -race ./...` passes
- [ ] Added/updated tests for the behavior change
- [ ] Updated relevant `docs/` page, if user-facing behavior changed
